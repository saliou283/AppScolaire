specVersion: "v1"
serviceId: "appscolaire"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "appscolaire-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
